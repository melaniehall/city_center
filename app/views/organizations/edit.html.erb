<%= form_for(@organization) do |f| %>

  <div><%= f.label :name %><br />
  <%= f.text_field :name, :autofocus => true %></div>

  <div><%= f.label :bio %><br />
  <%= f.text_area :bio %></div>

  <div><%= f.submit "Edit Profile" %></div>
<% end %>

<%= link_to "Back", :back %>

<h1>New Organization</h1>
<%= form_for @organization do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :bio %>
  <%= f.text_area :bio %>

  <%= f.submit "Create Organization" %>
<% end %>